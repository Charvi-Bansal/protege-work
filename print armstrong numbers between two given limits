#include <iostream>
#include<math.h>
using namespace std;
bool arm(int n){
    int i,save=n,t,sum=0;
    while(n!=0){
        sum=sum+pow(n%10,3);
        n/=10;
    }
    return save==sum;
}
int main(){
    int a,b;
    cin>>a>>b;
    for(int i=a;i<=b;i++){
       if(arm(i))
       cout<<i<<" is armstrong\n";
       else cout<<i<<" is NOT armstrong\n";
    }

    return 0;
}
